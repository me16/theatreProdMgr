{
  "name": "cue-stage-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^10.14.1"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
